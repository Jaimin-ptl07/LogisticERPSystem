global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files: []

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Basic Kafka metrics (if needed later, can add Kafka Exporter)
  # - job_name: "kafka"
  #   static_configs:
  #     - targets: ["kafka:9308"]
  #   scrape_interval: 10s
